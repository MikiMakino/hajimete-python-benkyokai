# 解答1：指定カテゴリのデータだけ取り出す

## まずカテゴリの種類を確認する

```python
print(整理済み["カテゴリ"].unique())
```

---

## コード（例：「文具」カテゴリ）

```python
整理済み[整理済み["カテゴリ"] == "文具"]
```

---

## 考え方

```
整理済み全体 → カテゴリ列を見て → 条件に合う行だけ残す
```

条件に使う列名と値を変えるだけで、見たいカテゴリに切り替えられます。
Python in Excel で学んだ絞り込みと同じ考え方です。
