# 解答2：金額が2000円以上の注文だけ取り出す

## コード

```python
整理済み[整理済み["金額"] >= 2000]
```

---

## 考え方

```
整理済み全体 → 金額列を見て → 2000以上の行だけ残す
```

数値条件は `>=`（以上）、`>`（より大きい）、`<=`（以下）なども使えます。

---

## かえてみる

金額が1000円以下の注文を探すには？

```python
整理済み[整理済み["金額"] <= 1000]
```
