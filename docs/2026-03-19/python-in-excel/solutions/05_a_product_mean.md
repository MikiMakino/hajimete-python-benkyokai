# 解答5：A商品の平均売上を求める

## コード

```python
sales_tbl[sales_tbl["商品"] == "A商品"]["売上"].mean()
```

---

## 考え方

次の2段階で考えると整理しやすいです。

```
① A商品の行だけを取り出す
   sales_tbl[sales_tbl["商品"] == "A商品"]

② その売上列の平均を求める
   ["売上"].mean()
```

練習4と同じく、絞り込みと集計の組み合わせです。
集計方法を `count()` から `mean()` に変えただけです。
