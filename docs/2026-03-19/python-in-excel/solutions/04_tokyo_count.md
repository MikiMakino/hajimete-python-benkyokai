# 解答4：東京支店の件数を数える

## コード

```python
sales_tbl[sales_tbl["支店"] == "東京"]["売上"].count()
```

---

## 考え方

次の2段階で考えると整理しやすいです。

```
① 東京支店の行だけを取り出す
   sales_tbl[sales_tbl["支店"] == "東京"]

② そのデータの件数を数える
   ["売上"].count()
```

絞り込みと集計を組み合わせた形です。
