# 解答3：売上が5000円以上のデータを表示する

## コード

```python
sales_tbl[sales_tbl["売上"] >= 5000]
```

---

## 考え方

売上列が5000以上の行だけを取り出しています。

```
表全体 → 売上列を見て → 5000以上の行だけ残す
```

文字条件（`== "値"`）と数値条件（`>= 数値`）の書き方のちがいに注目しましょう。
